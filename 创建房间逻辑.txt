服务器任务：首先需要创建一个自定义的会话协议！！ 然后就是写几个协议解析的类并做出对应的业务逻辑
对于房间的处理：
1.主持人创建房间时，赋予一个房间号，这里用httpPost请求想数据库增添房间数据并且返回对应的房间号。（问题1：如果是非主持人按了排位？游戏等怎么处理）
1.1 同时创建于服务器的一个基于房间号下的socket长连接。设想的是服务器对于以房间为单位管理不同房间号下的会话，是用开启线程还是构建Map的方式去保存对应
房间号数据呢 还是说需要多次去数据库拿数据？
1.2 主持人发指令即向服务器发指令：这里因为涉及到数据库的操作，或者以下排位等逻辑操作就都写在后台当中，这里的信息就需要一个判断（那么判断太多了是
不是很复杂或者用一个静态的常量去替代这些信息的请求呢？同时需要去对请求进行不同的判断）然后就是对应不同判断下的进行逻辑操作或者数据库的读写操作。
然后就是信息的返回。
1.3 返回操作 这里是需要对主持人作数据返回吗？还是说直接点击请求之后回到页面？
